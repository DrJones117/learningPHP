<?php
    // arithmetic operators
    // + - * / ** %

    $x = 10;
    $y = 2;
    $z = null;

    // $z = $x + $y;
    // $z = $x - $y;
    // $z = $x * $y;
    // $z = $x / $y;
    // $z = $x / $y;
    // $z = $x ** $y;
    $z = $x % $y;
    echo $z;

    echo "<br>";

    $counter = 0;

    $counter++;

    echo $counter;

    // You already know all of the increment and decrement operator.

    // Operator Precedence
    // ()
    // **
    // * / %
    // + -
?>